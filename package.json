{
  "name": "music-player-challenge",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter app dev",
    "build": "pnpm --filter app build",
    "preview": "pnpm --filter app preview",
    "lint": "pnpm --filter app lint",
    "dev:solution": "pnpm --filter solution dev",
    "build:solution": "pnpm --filter solution build"
  }
}
